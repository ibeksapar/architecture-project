import React from "react";
import { ToolbarContainer, ProjectTitle } from "./StyledComponents";

const Toolbar: React.FC = () => {
  return (
    <ToolbarContainer>
      <ProjectTitle>Simulation of Digital Logic Circuits</ProjectTitle>
    </ToolbarContainer>
  );
};

export default Toolbar;
